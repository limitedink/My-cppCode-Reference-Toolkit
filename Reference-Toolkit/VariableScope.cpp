#include <iostream>

int main()
{
    //Local variables = declared inside a function/block/house {}
    //Global variables = declared outside of all functions.
    // tend to be at the top of a program's code
    // :: is the scope resolution operator to force a function to use
    //the global version of a variable instead of the local version
    //try not to use global variables as they pollute the global namespace
    
    return 0;
}